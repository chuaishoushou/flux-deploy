# 更新日志

## 1.3.3 — 2026-05-27

1. 邮件模板编辑器改用系统浏览器打开，并重新设计默认模板（预置常用字段 + 自动变量）。
2. 优化粘贴兼容，可与企业微信等邮件往返编辑。
3. 新增邮件变量「更新模式」。
4. 部署目标列表新增文件体积和修改时间，方便核对版本。
5. 优化更新流程：人工确认步骤统一前移，确认后不再中途弹窗打断。
6. 修复版本记录时间：取包、传包时间分别记录，不再混淆。

## 1.3.2 — 2026-05-26

1. 优化版本记录文件名识别：自动跨版本号识别同一个包的版本记录，旧文件（如 `scev6-utils-tms_update_notes.txt`）与新文件（如 `scev6-utils-tms-9.0.0-SNAPSHOT_update_notes.txt`）视为同一份，不再因版本号差异重复创建。同时不会误中同前缀的兄弟包（如更新 `scev6-utils` 时不会误识别 `scev6-utils-tms`）。
2. 修复版本记录文件命名：新建版本记录文件不再参考其它目录或上层目录的命名风格，统一使用 `<包全名>_update_notes.txt` 标准命名。
3. 新增版本记录冲突检测：勾选「更新版本记录」后，预检阶段会检查目标包目录是否已存在多个匹配的版本记录文件，命中即弹窗提示用户先手动清理，避免上传完成才发现冲突。
4. 优化「目标包疑似备份目录」安全警告：调整为最前置弹窗，命中一个备份目录即提示，避免误勾备份根目录时还要等几千个文件空检完成。
5. 修复部署确认对话框宽度过宽：多目标时远程路径列表改为滚动框显示，对话框宽度不再被超长路径撑爆。
6. 修复部署确认对话框：仅选择 WAR 嵌入目标（未选主 JAR）时，目标包和远程路径不再显示「未选择」。
7. 修复目标包树形勾选框父子联动：取消最后一个子节点勾选时，父级目录会同步取消勾选。
8. 修复预检失败后再次点击「执行更新」跳过校验的问题，确保每次操作都重新执行预检。
9. 优化邮件模板编辑器：工具栏「邮件变量」下拉移到最前并改为「`${变量名}` + 实际值示例」直观格式；新增 `更新包路径` / `备份包` / `备份包路径` 三个变量；修复变量复制为空、Ctrl+A 全选复制后变量丢失的问题；默认模板内容与弹窗布局重新调整。

## 1.3.1 — 2026-05-19

1. 新增邮件模板功能。
2. 插件UI优化，按钮布局调整。
3. 安全性优化，增加编译文件时间是否早于目标文件，防止更新旧class文件。

## 1.3.0 — 2026-05-14

1. 修复 FTP 目标文件超过 2000+ 文件时，目标列表部分丢失。
2. 优化版本记录_notes.txt内容格式，不再使用|拼接，使用标准格式。
3. 新添加工程项目时，刷新可以自动加载新项目。
4. 插件UI页面优化，交互操作优化。

## 1.2.9 — 2026-05-12

1. 修复版本记录 txt 命名不规范导致重复，使用包名 + txt 匹配。优先使用当前目录已存在 txt 命名格式，保证格式统一，当前目录不存在时最多向上查找 3 层目录。都不存在时使用默认标准格式命名。
   兼容格式例如：
   - `scev6-utils-tms-9.0.0-SNAPSHOT_update_notes.txt`
   - `scev6-utils-tms-9.0.0-SNAPSHOT.jar_update_notes.txt`
   - `scev6-utils-tms-9.0.0-SNAPSHOT_update_note.txt`
   - `scev6-utils-tms-9.0.0-SNAPSHOT_notes.txt`

## 1.2.8 — 2026-05-08

1. 修复批量更新时，可能出现命中错误包，导致更新错误包。
2. 取消插件编译功能，需要先手动执行 maven 编译 / 打包后才能执行更新，未编译会弹窗提示。静态文件不需要编译。
3. 优化日志输出。增加日志持久化，本地目录：`~/.flux-deploy/logs`。
4. 增加误勾选校验，当目标包目录存在 `bak`、`backup` 等目录时，会弹窗提示，防止误更新。
5. 更新性能优化，50 个包原更新时间需要 55 分钟，现在仅需 35 分钟。

## 1.2.7 — 2026-05-04

### 新功能
1. **自定义备份目录**：项目备份文件夹支持手动选择路径，不再固定在远端目录下。
2. **目标列表搜索**：源工程、部署目标列表均增加搜索框，支持按关键词过滤。
3. **多级动态筛选**：目标 / 系统下拉支持联动多级筛选。
4. **批量并行更新**：批量更新支持并行执行，默认线程数 3、单包失败重试 3 次。配置文件位置 `~/.flux-deploy/config.toml`。
5. **失败隔离**：批量遇错时跳过并回滚问题包，其他包继续更新，全程记录日志。
6. **动态编译跳过**：变更全部为静态资源时跳过 mvn package；含 .java 改动时强制编译。
7. **JDK / Maven 解析**：JDK 走 pom 中 `release/target/source`（含父级 pom 继承），Maven 用 IDEA 配置的 Maven Home，不再依赖 `JAVA_HOME / MAVEN_HOME` 环境变量。Windows / macOS 兼容。
8. **日志重排版**：增加阶段耗时、当前状态、问题原因、操作建议、更新清单；取消强制预检，可按需触发。
9. **手动停止可选模式**：中途按停止时弹窗，三种结束方式：继续 / 保留已完成包并停止 / 全部回滚并停止。
10. **FTP 无感重连**：长连接被服务端断开 / 网络瞬断时自动重连，断点续传，3 次自动重试用尽后弹"重试 / 结束"窗。
11. **版本记录文件兼容**：同时识别 `xxx.war_update_note.txt` 和 `xxx_update_note.txt`；存在则沿用，不存在则新建 `xxx.war_update_note.txt`。
12. **备份目录可见**：取消"自动隐藏备份目录"逻辑，FTP 浏览树展示路径下所有包，是否勾选由用户决定。

### 修复
- 串行嵌入路径接入 `failureStrategy`：单包失败按 `ISOLATED` 跳过继续，与并行路径行为对齐。
- 嵌入失败提示按根因区分：WAR 内不含目标 JAR 时提示"检查部署目标是否选错"，区别于网络/系统问题的"建议重新部署"。
- 残留锁支持 TTL：锁文件名内嵌过期时间，超时自动清理；Stage 0 自检兼顾 `.__UPLOADING__` 残留临时文件。
- FTP 错误分类扩展：`421/425/426/450 / Connection reset / Broken pipe / Connection closed without indication` 等服务端瞬态错误纳入自动重试。
