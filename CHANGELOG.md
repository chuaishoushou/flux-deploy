# 更新日志

## 1.4.0 — 2026-09-23

1. 修复 Vue 更新时网络不稳定导致更新包被投放到系统根目录的问题。
2. Vue 更新只认服务包目录，取消 zip 投放；定位不到目录时明确报错，可刷新重试或手动指定目录。
3. Vue 模块改为整体切换发布：新内容传完校验无误后瞬间切换，不再逐文件覆盖，中断时线上保持完整旧版本。
4. 上传支持断点续传：中断后自动退避重连并从断点继续，重试用尽由你选择继续或结束。
5. 传输每 5 秒显示一次进度：总大小、已传、剩余、速率、预计时间。
6. 修复回滚未完成仍清理备份导致原始版本丢失的问题：有文件未恢复时保留备份，可再次回滚重做。
7. 备份、版本记录等远端写入改为传完校验无误才替换，并逐一核对完整性，不通过立即中止。
8. 新增并发变更拦截：备份后被他人改动的文件不再被覆盖。
9. Vue 版本记录写入失败改为整体回滚，不再出现「更新成功但没写记录」。

## 1.3.9 — 2026-08-13

1. 新增 Vue 前端工程支持：「工程」下拉可搜索到 Vue 模块式 Web 工程（识别依据：工程根有 serve.yaml、package.json 声明 zip:module 打包脚本且存在 src/modules 或 dist/umd 模块目录形态），显示名带线上上下文名（如 `sce-vtms-m01-web (tm01webVue)`）。选中后源面板改为业务模块勾选列表，标注三种产物状态（未构建 / 已过期 / 产物时间），git 检测到 `src/modules/<模块号>/` 有变更的模块默认勾选。执行更新时对勾选模块中「未构建 / 已过期」的**自动构建**（调用工程自带 CLI 的 `build:one --force`，模块号 stdin 喂入、构建输出进运行日志、失败即中止且远端零变更；该命令无 git 副作用，带 git reset 的全量构建命令绝不会被自动执行）。
2. Vue 目标支持「目录直更」（服务包解包形态，主流程）：选中 Vue 工程后自动在系统目录下探测与 content 同名的服务包目录（如 `A06SysBizWebVue/`），目标树把选中模块显示为「模块目录（目录更新）」条目并默认勾选——执行更新即把本地 `dist/umd/<模块号>/` 逐文件覆盖到远端模块目录：先把**整个工程包**逐文件镜像备份到 backup 目录（FTP 会话复用 + 断线重连 + 进度日志），上传时子目录按需创建、manifest.json 最后传（把"半新半旧"窗口压到最小），随后逐文件核对远端字节数，失败自动回滚（被覆盖文件从备份写回、本次新增文件删除）；远端存在而本地产物没有的历史文件保留不动并在日志说明。模块目录不存在时按「新建投放」处理。版本记录按**工程包一份**：`<content>_update_notes.txt` 放在服务包目录旁边（与 jar/war 记录放包旁的惯例一致），一次更新追加一条记录。
3. Vue 目标同时支持 zip 形态（兜底）：目标树会列出 `.zip` 文件，与选中模块命名匹配的（兼容旧版带时间戳命名）默认勾选，走与 jar/war 一致的备份 / 上传 / 哈希校验 / 回滚链路；FTP 上既无服务包目录也无 zip 时，注入「（新建）」虚拟 zip 节点完成首次投放。两种形态不可混选。
4. Vue 两档更新模式与后端语义对齐：「增量更新」（默认）= 左侧展示 `src/modules/` 全量**源文件树**（git 变更项按 [A]/[M]/[D] 标色并默认勾选，直接看出自己改了哪几个文件），勾选文件所在的模块整体参与更新（源文件与编译产物非一一对应，最小闭环单元是模块，执行时自动映射为最新编译产物）；「整包更新」= 整个工程包（全部业务模块）覆盖更新，模块列表全选只读，未构建/过期的全部自动构建。
5. Vue 目标树的模块目录条目自动展开为**远端现状文件视图**（只列 FTP 上当前真实存在的文件，按 dialogs/、submodules/ 等真实子目录层级嵌套，行尾显示远端大小与时间，与 jar/war 列表同款风格）：勾选语义 = 本次更新是否允许覆盖该文件，取消若干即单文件更新（排除式选择：本地新增的产物文件不受影响、自动上传并在运行日志体现，不会漏传 manifest 引用的新文件）；文件全勾时父目录正确显示全选（"保留"概念已移除）。备份始终镜像整个模块目录，保证回滚是完整快照。
6. 邮件模板新增 `${更新vue包}` 变量（按 .zip 后缀分组），已有模板不受影响。

## 1.3.8 — 2026-08-04

1. 目标包列表取消过滤隐藏：不再按源产物类型 / 文件名隐藏未命中的包，FTP 上扫描到的全部 jar / war 和文件夹都完整显示，由你手动勾选要更新的目标；与源工程同名的包仍会默认勾选。勾选的同类型包（JAR 源勾 jar、WAR 源勾 war）都作为主目标直接更新，不再要求与源产物同名；JAR 源勾选的 war 仍是嵌入更新。
2. 修复系统目录下部分文件夹（及其中的包）在目标列表凭空消失的问题：扫描配额原按「条目数」计算，静态资源目录（如 Vue 前端解包后的数千个小文件）会把配额耗尽，导致字母序靠后的文件夹整个扫不到。现配额只计目录数、不计普通文件，shared-wms / timer-* 这类文件夹不会再因同级大目录而丢失。
3. 目标包列表改为按目录层级懒加载：选择系统后只读取根层（一次 FTP 请求），文件夹默认折叠、点开哪个才去 FTP 读取哪个，不再一次性递归扫描整个系统子树——大系统下加载明显变快，也不会再被深层大目录拖慢。展开过的层级会缓存，点「刷新」重新读取。勾选尚未展开的文件夹会自动加载其内容并全部勾选，保持「勾文件夹＝选中其下全部包」的语义。
4. 文件夹名称不再按「其中有没有包」置灰，样式统一。

## 1.3.7 — 2026-07-27

1. 新增版本记录文件手动选择：目标包在远端目录里匹配不到对应的版本记录 txt 时，不再静默按标准命名新建，改为弹窗让你从该目录已有的 txt 里选一个（本次记录追加到该文件末尾），或修改文件名新建一个，也可直接取消本次更新；避免与目录里命名差异较大的已有记录文件形成两份。弹窗在预检阶段弹出，此时远端还没有任何写入，取消无副作用；手动指定的文件在回滚时也能精确找回并回退记录。
2. 统一全插件弹窗风格：按钮位置交给 IDE 排列，与 IDE 自身弹窗一致，不再出现「取消在前」「确定在前」并存；回滚、删除账号、不备份直接更新这类不可逆操作默认聚焦「取消」，回车不会误触发；按钮文案统一为动词短语（确认执行 / 确认回滚 / 开始打包 / 删除账号），不再出现系统英文 OK / Cancel；弹窗宽度、边距、说明文字风格全部拉齐。
3. FTP 登录框重做为标准弹窗，并补上端口号数字校验——原来端口输入非数字会直接报错中断。
4. 修复 JAR 源默认勾选的目标包所在目录不自动展开的问题：命中的同名包在嵌套目录（如 `TMS-V9.0.0-P11/shared-tms`）里时，现在会自动展开到该包，不必再逐层手动点开；原先只有 WAR 源有这个行为。
5. 修复 IDE 未内置或未启用浏览器内核（JCEF）时的一连串报错：打开部署工具窗口弹「Unhandled exception in EDT」、「使用手册」「版本记录」空白或报错、「通知邮件」编辑器直接抛异常中断。现改为安全探测——文档页自动回退到内置渲染，内容照常查看（内核初始化中途失败也会回退）；邮件编辑器改为弹窗提示，引导开启 `ide.browser.jcef.enabled` 后重启 IDE 再用。
6. 本地打包确认弹窗不再列出「编译项目」步骤（插件已不触发编译），只列实际执行的步骤；「Note 文件冲突」弹窗标题统一为「版本记录文件冲突」，与其他位置叫法一致。

## 1.3.6 — 2026-07-11

1. 新增 CSV 行级增量更新：增量更新 initdata CSV 时不再整份覆盖，只把本地未提交的改动行（新增 / 修改 / 删除）同步到目标包对应行，新增行按本地文件中的位置插入；目标包里其他人尚未提交 Git 的行数据不会再被覆盖丢失。重复执行更新不会产生重复行。
2. CSV 业务主键自动识别：从工程内 tablefieldlist 字段清单自动解析每张表的主键列（支持复合主键），仅用于定位本次新增 / 修改 / 删除行；插件不再对 CSV 重复内容做校验或告警。
3. 无法按行合并的场景自动退回原有整份覆盖并在日志说明原因（文件无 Git 变更记录、表头结构不一致、识别不了业务主键、编码异常等），更新流程不中断。
4. 更新日志新增每个 CSV 的合并摘要：新增 / 替换 / 删除行数与保留的目标包行数。
5. 修复回滚安全性问题：新包已上传后部署失败，回滚不再出现"提示成功但远端实际未还原"——有备份时用备份覆盖还原，无备份时先删除新包再还原锁包，两者都没有时如实报告需人工处理。
6. 回滚业务包时同步回退版本记录 txt：按原始字节还原（保留原编码），本次新建的直接删除，不再出现包已回退但版本记录还停留在新内容的不一致。
7. 修复版本记录文件重名冲突时部署中断却不回滚的问题：命中多个候选版本记录文件现在会正确触发回滚，不再在远端残留锁包和新包。
8. 修复部署已成功、仅清理锁包失败时被误判为失败并错误回退新版本的问题：删除锁包失败自动重试 3 次，仍失败按"部署完成 + 告警"处理，残留锁由下次部署自动清理。
9. 编译产物过期检查收紧：源码修改时间等于编译产物时间也判为需要重新编译，防止上传未重新编译的旧文件。
10. 低分辨率屏幕适配：「执行操作」与「运行日志」两栏支持一键联动折叠成细条，给源工程与部署目标列表腾出高度，折叠状态重启后保持记忆。
11. 界面字号跟随 IDE 全局字体设置缩放，高分屏下卡片与图标同步高清缩放；窄面板下提示文字自动省略、悬停可查看完整内容；修复邮件模板弹窗可被重复打开多个的问题。
12. 修复编辑器中未保存的修改不进包的问题：点击「执行更新」或本地打包时自动先保存所有未保存的文件，避免 Git 面板显示有变更、实际打进包的却是改动前的旧内容。
13. 目标包列表完整显示目录结构：更新 jar / war 包时，包仍按原规则过滤隐藏，但目录不再跟着一起消失——FTP 上有哪些目录就列哪些，其中没有可部署包的目录置灰且不可勾选，不会再出现「看到的目录与 FTP 实际结构对不上」的错觉。

## 1.3.5 — 2026-06-17

1. 更新 WAR 包时，目标包列表显示全部 WAR 包并隐藏 JAR 包，不再要求产物名与 FTP 目标包名一致；与源工程同名的 WAR 默认勾选并自动展开所在目录，解决两者命名不同导致选不到目标包的问题。
2. 隐藏「本地打包」按钮。

## 1.3.4 — 2026-06-02

1. 邮件模板的「默认模板」现在可以自由编辑，且不会被插件升级覆盖；点「新建」或「恢复默认」时会套用最新的内置模板。
2. 邮件变量优化：精简了变量下拉列表，新增「FTP版本来源」「更新jar包」「更新war包」等变量；变量可直接选中、修改、删除和手动输入，编辑更顺手；点「导入数据」后变量会在正文中直接显示为实际值，方便核对。
3. 支持更新项目根目录下的包，取消必须选择系统。
4. 修复版本记录文件txt编码不一致导致写入失败，支持GBK等其他编码。自动转换utf-8。
5. 修复更新 WAR 包时，未修改文件的修改时间也被更新的问题。

## 1.3.3 — 2026-05-27

1. 重新设计邮件默认模板（预置常用字段 + 自动变量）。
2. 优化粘贴兼容，可与企业微信等邮件往返编辑。
3. 新增邮件变量「更新模式」。
4. 部署目标列表新增文件体积和修改时间，方便核对版本。
5. 优化更新流程：人工确认步骤统一前移，确认后不再中途弹窗打断。
6. 修复版本记录时间：取包、传包时间分别记录，不再混淆。

## 1.3.2 — 2026-05-26

1. 优化版本记录文件名识别：自动跨版本号识别同一个包的版本记录，旧文件（如 `scev6-utils-tms_update_notes.txt`）与新文件（如 `scev6-utils-tms-9.0.0-SNAPSHOT_update_notes.txt`）视为同一份，不再因版本号差异重复创建。同时不会误中同前缀的兄弟包（如更新 `scev6-utils` 时不会误识别 `scev6-utils-tms`）。
2. 修复版本记录文件命名：新建版本记录文件不再参考其它目录或上层目录的命名风格，统一使用 `<包全名>_update_notes.txt` 标准命名。
3. 新增版本记录冲突检测：勾选「更新版本记录」后，预检阶段会检查目标包目录是否已存在多个匹配的版本记录文件，命中即弹窗提示用户先手动清理，避免上传完成才发现冲突。
4. 优化「目标包疑似备份目录」安全警告：调整为最前置弹窗，命中一个备份目录即提示，避免误勾备份根目录时还要等几千个文件空检完成。
5. 修复部署确认对话框宽度过宽：多目标时远程路径列表改为滚动框显示，对话框宽度不再被超长路径撑爆。
6. 修复部署确认对话框：仅选择 WAR 嵌入目标（未选主 JAR）时，目标包和远程路径不再显示「未选择」。
7. 修复目标包树形勾选框父子联动：取消最后一个子节点勾选时，父级目录会同步取消勾选。
8. 修复预检失败后再次点击「执行更新」跳过校验的问题，确保每次操作都重新执行预检。
9. 优化邮件模板编辑器：工具栏「邮件变量」下拉移到最前并改为「`${变量名}` + 实际值示例」直观格式；新增 `更新包路径` / `备份包` / `备份包路径` 三个变量；修复变量复制为空、Ctrl+A 全选复制后变量丢失的问题；默认模板内容与弹窗布局重新调整。

## 1.3.1 — 2026-05-19

1. 新增邮件模板功能。
2. 插件UI优化，按钮布局调整。
3. 安全性优化，增加编译文件时间是否早于目标文件，防止更新旧class文件。

## 1.3.0 — 2026-05-14

1. 修复 FTP 目标文件超过 2000+ 文件时，目标列表部分丢失。
2. 优化版本记录_notes.txt内容格式，不再使用|拼接，使用标准格式。
3. 新添加工程项目时，刷新可以自动加载新项目。
4. 插件UI页面优化，交互操作优化。

## 1.2.9 — 2026-05-12

1. 修复版本记录 txt 命名不规范导致重复，使用包名 + txt 匹配。优先使用当前目录已存在 txt 命名格式，保证格式统一，当前目录不存在时最多向上查找 3 层目录。都不存在时使用默认标准格式命名。
   兼容格式例如：
   - `scev6-utils-tms-9.0.0-SNAPSHOT_update_notes.txt`
   - `scev6-utils-tms-9.0.0-SNAPSHOT.jar_update_notes.txt`
   - `scev6-utils-tms-9.0.0-SNAPSHOT_update_note.txt`
   - `scev6-utils-tms-9.0.0-SNAPSHOT_notes.txt`

## 1.2.8 — 2026-05-08

1. 修复批量更新时，可能出现命中错误包，导致更新错误包。
2. 取消插件编译功能，需要先手动执行 maven 编译 / 打包后才能执行更新，未编译会弹窗提示。静态文件不需要编译。
3. 优化日志输出。增加日志持久化，本地目录：`~/.flux-deploy/logs`。
4. 增加误勾选校验，当目标包目录存在 `bak`、`backup` 等目录时，会弹窗提示，防止误更新。
5. 更新性能优化，50 个包原更新时间需要 55 分钟，现在仅需 35 分钟。

## 1.2.7 — 2026-05-04

### 新功能
1. **自定义备份目录**：项目备份文件夹支持手动选择路径，不再固定在远端目录下。
2. **目标列表搜索**：源工程、部署目标列表均增加搜索框，支持按关键词过滤。
3. **多级动态筛选**：目标 / 系统下拉支持联动多级筛选。
4. **批量并行更新**：批量更新支持并行执行，默认线程数 3、单包失败重试 3 次。配置文件位置 `~/.flux-deploy/config.toml`。
5. **失败隔离**：批量遇错时跳过并回滚问题包，其他包继续更新，全程记录日志。
6. **动态编译跳过**：变更全部为静态资源时跳过 mvn package；含 .java 改动时强制编译。
7. **JDK / Maven 解析**：JDK 走 pom 中 `release/target/source`（含父级 pom 继承），Maven 用 IDEA 配置的 Maven Home，不再依赖 `JAVA_HOME / MAVEN_HOME` 环境变量。Windows / macOS 兼容。
8. **日志重排版**：增加阶段耗时、当前状态、问题原因、操作建议、更新清单；取消强制预检，可按需触发。
9. **手动停止可选模式**：中途按停止时弹窗，三种结束方式：继续 / 保留已完成包并停止 / 全部回滚并停止。
10. **FTP 无感重连**：长连接被服务端断开 / 网络瞬断时自动重连，断点续传，3 次自动重试用尽后弹"重试 / 结束"窗。
11. **版本记录文件兼容**：同时识别 `xxx.war_update_note.txt` 和 `xxx_update_note.txt`；存在则沿用，不存在则新建 `xxx.war_update_note.txt`。
12. **备份目录可见**：取消"自动隐藏备份目录"逻辑，FTP 浏览树展示路径下所有包，是否勾选由用户决定。

### 修复
- 串行嵌入路径接入 `failureStrategy`：单包失败按 `ISOLATED` 跳过继续，与并行路径行为对齐。
- 嵌入失败提示按根因区分：WAR 内不含目标 JAR 时提示"检查部署目标是否选错"，区别于网络/系统问题的"建议重新部署"。
- 残留锁支持 TTL：锁文件名内嵌过期时间，超时自动清理；Stage 0 自检兼顾 `.__UPLOADING__` 残留临时文件。
- FTP 错误分类扩展：`421/425/426/450 / Connection reset / Broken pipe / Connection closed without indication` 等服务端瞬态错误纳入自动重试。
